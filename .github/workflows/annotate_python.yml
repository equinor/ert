name: Annotate Python

on: [pull_request]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  annotate-python-linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: -1
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: "pipenv"
      - run: curl https://raw.githubusercontent.com/pypa/pipenv/master/get-pipenv.py | python
      - run: pipenv install --ignore-pipfile
      - name: install project
        run: pipenv run pip install .
      - name: find changed files
        id: find_changed_files
        run: echo ::set-output name=changed_files::$(git diff --name-only ${{github.sha}} ${{github.event.pull_request.base.sha}} | tr ' ' '\n' |  xargs ls -d 2>/dev/null | grep -E ".py$" | tr '\n' ' ')
      - run: echo ::add-matcher::.github/flake8-matcher.json
      - name: run flake8
        run: pipenv run flake8 --exit-zero ${{steps.find_changed_files.outputs.changed_files}}
        if: steps.find_changed_files.outputs.changed_files != ''
      - run: echo ::add-matcher::.github/mypy-matcher.json
      - name: run mypy
        run: pipenv run mypy ${{steps.find_changed_files.outputs.changed_files}} --config-file .mypy-annotate.ini
        if: steps.find_changed_files.outputs.changed_files != ''
        continue-on-error: true
