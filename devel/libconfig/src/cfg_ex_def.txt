required key GRIDFILE
{
  data_type   filename 
  help_text   "The keyword GRIDFILE specifies the Eclipse gridfile to be used."
}



type GENKW
{
  help_text   "The keyword GENKW can be used to parameterize any quantity in the Eclipse data file."

  required key TEMPLATE_FILE
  {
    data_type   filename
    help_text   "Template file used. Matching expressions inside < > brackets are replaced with values."
  }

  required key ECL_INC_FILE
  {
    data_type   string
    help_text   "Filename to use when including during simulations."
  }

  required key CONFIG_FILE
  {
    data_type   string
    help_text   "Filename to the configuration file. See wiki for syntax."
  }
}
