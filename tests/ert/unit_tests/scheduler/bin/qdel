#!/usr/bin/env bash
set -e

jobid=$1
jobdir="${PYTEST_TMP_PATH:-.}/mock_jobs/${jobid}"

if ! [ -f "${jobdir}/pid" ]
then
    echo "No such job ${jobid}" >&2
    exit 1
fi

pid=$(cat "${jobdir}/pid")
kill $pid
