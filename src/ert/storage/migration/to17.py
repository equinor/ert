from pathlib import Path

info = (
    "Add localization attributes to summary observations. No change to current "
    "storage, aka no migration. Bumping to 17 to indicate new feature in storage."
)


def migrate(path: Path) -> None:
    pass
