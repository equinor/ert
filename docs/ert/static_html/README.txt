The files in this directory are copied to a directory _static/ in the sphinx
output directory. Static files like css and images can be put here.

Observe that the files in this directory is only copied over when the sphinx
output directory is created, i.e. to get changes to these files through you must
delete the output directory and rerun sphinx-build.
